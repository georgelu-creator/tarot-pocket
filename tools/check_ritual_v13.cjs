/* Real mobile taps, held motion, all 78 choices, and durable staged reveals. */
const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),http=require('node:http');
const {chromium}=require('playwright'),root=path.resolve(__dirname,'..'),KEY='tarot-reading-v3';
const state=p=>p.evaluate(k=>JSON.parse(localStorage.getItem(k)),KEY);
const click=(p,a,i)=>p.locator(`[data-reading="${a}"]${i===undefined?'':`[data-index="${i}"]`}`).first().click();
(async()=>{
 let server,url=process.env.DEMO_URL;
 if(!url){server=http.createServer((req,res)=>{
  const rel=decodeURIComponent(new URL(req.url,'http://localhost').pathname).slice(1)||'index.html';
  if(rel==='locales/en.js'){const dict=Object.assign({},...fs.readdirSync(path.join(root,'locales')).filter(n=>/^en-.*\.json$/.test(n)).sort().map(n=>JSON.parse(fs.readFileSync(path.join(root,'locales',n),'utf8'))));res.setHeader('Content-Type','text/javascript');res.end('window.TAROT_EN='+JSON.stringify(dict));return;}
  if(!/\.(html|js|css|json|webp|png|svg|webmanifest)$/.test(rel)||/^(server|node_modules|\.)/.test(rel)){res.statusCode=403;res.end();return;}
  const file=path.join(root,rel);if(!file.startsWith(root+path.sep)||!fs.existsSync(file)||!fs.statSync(file).isFile()){res.statusCode=404;res.end();return;}
  res.setHeader('Content-Type',({'.html':'text/html; charset=utf-8','.js':'text/javascript','.css':'text/css','.json':'application/json','.webp':'image/webp','.png':'image/png','.svg':'image/svg+xml'})[path.extname(file)]||'application/octet-stream');fs.createReadStream(file).pipe(res);
 });await new Promise(r=>server.listen(0,'127.0.0.1',r));url=`http://127.0.0.1:${server.address().port}/index.html`;}
 const browser=await chromium.launch(require('./browser_options.cjs'));
 try{
  const errors=[],ctx=await browser.newContext({viewport:{width:390,height:844},isMobile:true,hasTouch:true}),p=await ctx.newPage();p.on('pageerror',e=>errors.push(e.message));await p.goto(url);await p.locator('.bottomnav [data-page=reading]').click();await p.locator('[data-reading=guide][data-value=three]').click();await p.locator('[data-reading=use-spread]').click();await click(p,'start');
  const original=await state(p),raw=structuredClone(original);
  const button=p.locator('[data-reading=motion-continue]');assert(await button.isDisabled(),'normal motion leaves time to watch');await p.waitForTimeout(220);const a=await p.locator('.ritual-pack>.reading-back').first().evaluate(e=>getComputedStyle(e).transform);await p.waitForTimeout(350);const b=await p.locator('.ritual-pack>.reading-back').first().evaluate(e=>getComputedStyle(e).transform);assert.notEqual(a,b);
  await p.waitForFunction(()=>!document.querySelector('[data-reading="motion-continue"]').disabled);await p.waitForTimeout(160);assert.equal((await state(p)).draft.phase,'shuffling','finished animation never takes control away from the user');await p.screenshot({path:'/tmp/tarot-v13-shuffle.png',fullPage:false});await click(p,'motion-continue');
  await click(p,'cut',52);assert(await p.locator('[data-reading=motion-continue]').isDisabled());const cut=await state(p);assert.deepEqual(cut.draft.pool,[...original.draft.pool.slice(52),...original.draft.pool.slice(0,52)]);await p.waitForFunction(()=>!document.querySelector('[data-reading="motion-continue"]').disabled);assert.equal((await state(p)).draft.phase,'cutting');await click(p,'motion-continue');
  const seen=new Set();for(let page=0;page<13;page++){
   const indices=await p.locator('[data-reading=pick]').evaluateAll(es=>es.map(e=>Number(e.dataset.index)));assert.equal(indices.length,6);indices.forEach(i=>seen.add(i));assert.equal(await p.locator('.ritual-card-window img').count(),0);
   const boxes=await p.locator('[data-reading=pick]').evaluateAll(es=>es.map(e=>{const b=e.getBoundingClientRect();return {x:b.x,y:b.y,w:b.width,h:b.height};}));assert(boxes.every(b=>b.w>=60&&b.h>=95));for(let i=0;i<boxes.length;i++)for(let j=i+1;j<boxes.length;j++){const a=boxes[i],b=boxes[j];assert(a.x+a.w<=b.x+.5||b.x+b.w<=a.x+.5||a.y+a.h<=b.y+.5||b.y+b.h<=a.y+.5,'touch rectangles must not overlap');}
   if(page<12)await click(p,'pick-page',page+1);
  }assert.equal(seen.size,78);assert(await p.locator('[data-reading=pick-page][data-index="13"]').isDisabled());
  await click(p,'pick',77);assert.deepEqual((await state(p)).draft.picked,[]);assert.equal((await state(p)).draft.pendingPick,77);assert.equal(await p.locator('.candidate').count(),1);await p.screenshot({path:'/tmp/tarot-v13-select.png',fullPage:true});
  await p.reload();await p.locator('.bottomnav [data-page=reading]').click();assert.equal((await state(p)).draft.pendingPick,77);assert.equal((await state(p)).draft.drawPage,12);assert.deepEqual((await state(p)).draft.pool,cut.draft.pool);await p.locator('[data-reading=pick-confirm]').evaluate(e=>{e.click();e.click();});assert.deepEqual((await state(p)).draft.picked,[77]);
  // The whole table shell is stable when a candidate is lifted or confirmed.
  await p.locator('.reading-sticky-head').evaluate(e=>e.dataset.identity='kept');await click(p,'pick-page',11);for(let page=10;page>=0;page--)await click(p,'pick-page',page);await click(p,'pick',0);assert.equal(await p.locator('.reading-sticky-head').getAttribute('data-identity'),'kept');await click(p,'pick',0);assert.equal((await state(p)).draft.pendingPick,null,'second tap cancels candidate');await click(p,'pick',0);await click(p,'pick-confirm',0);await click(p,'pick',1);await click(p,'pick-confirm',1);assert.equal((await state(p)).draft.phase,'reveal');
  for(let i=0;i<3;i++){
   assert.equal(await p.locator('.ritual-spotlight img').count(),0);await click(p,'reveal-next',i);assert.equal(await p.locator('.ritual-spotlight img').count(),1);assert.equal(await p.locator('.reading-interpretation').count(),0);assert(await p.locator('[data-reading=reveal-place]').isDisabled(),'leave time for the card turn');await p.waitForFunction(()=>!document.querySelector('[data-reading="reveal-place"]').disabled);await p.waitForTimeout(130);assert.equal(await p.locator('.ritual-reveal-back').count(),0,'temporary back is removed so it cannot occlude the face');assert.equal(await p.locator('.ritual-spotlight.is-revealing').count(),0,'final face is no longer in a 3D animation layer');assert.equal((await state(p)).draft.phase,'reveal','viewing a card has no automatic time limit');
   if(i===2){await p.screenshot({path:'/tmp/tarot-v13-reveal.png',fullPage:false});await p.reload();await p.locator('.bottomnav [data-page=reading]').click();assert.equal((await state(p)).draft.phase,'reveal');assert.equal((await state(p)).draft.revealPending,2);assert.equal(await p.locator('.ritual-spotlight img').count(),1);}
   await click(p,'reveal-place',i);
  }
  const normal=await state(p);assert.equal(normal.draft.phase,'read');assert.equal(await p.locator('.reading-slot.active').count(),0);assert.equal(await p.locator('.reading-interpretation').count(),0);assert.equal(await p.locator('[data-reading-ai]').count(),1,'whole-spread result is the primary report');const connected=await p.evaluate(()=>!window.TAROT_STANDALONE&&!!window.TAROT_AI_CONFIG?.endpoint);assert.equal(await p.locator('[data-reading=ai-request]').count(),connected?1:0,'only a connected web edition exposes an AI request');if(await p.evaluate(()=>!!window.TAROT_STANDALONE))assert.match(await p.locator('[data-reading-ai]').innerText(),/独立离线文件/);await click(p,'card',0);await click(p,'card',0);assert.equal(await p.locator('.reading-interpretation').count(),0);
  // Replay the exact same already-randomized pool with reduced motion and the skip actions.
  const fast=await browser.newContext({viewport:{width:320,height:740},isMobile:true,hasTouch:true,reducedMotion:'reduce'}),q=await fast.newPage();q.on('pageerror',e=>errors.push(e.message));await q.addInitScript(({KEY,raw})=>localStorage.setItem(KEY,JSON.stringify(raw)),{KEY,raw});await q.goto(url);await q.locator('.bottomnav [data-page=reading]').click();await q.locator('[data-language-toggle]').click();
  const English=async()=>{await q.waitForTimeout(30);const text=await q.locator('body').evaluate(el=>{const walker=document.createTreeWalker(el,NodeFilter.SHOW_TEXT);let n,out=[];while(n=walker.nextNode())if(/[\u3400-\u9fff]/.test(n.nodeValue)&&n.parentElement.checkVisibility()&&!n.parentElement.closest('[data-i18n-ignore],script,style'))out.push(n.nodeValue);return out;});assert.deepEqual(text,[]);assert(await q.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1));};
  await English();await click(q,'skip-animation');await English();await click(q,'cut',52);await English();await click(q,'skip-animation');await English();for(let page=1;page<13;page++)await click(q,'pick-page',page);await click(q,'pick',77);await English();await click(q,'pick-confirm',77);for(let page=11;page>=0;page--)await click(q,'pick-page',page);for(const i of [0,1]){await click(q,'pick',i);await click(q,'pick-confirm',i);}await English();for(let i=0;i<3;i++){await click(q,'reveal-next',i);await English();await click(q,'reveal-place',i);}const reduced=await state(q);assert.deepEqual(reduced.draft.pool,normal.draft.pool);assert.deepEqual(reduced.draft.picked,normal.draft.picked);assert.deepEqual(reduced.draft.revealed,normal.draft.revealed);assert.equal(reduced.draft.phase,'read');assert.deepEqual(errors,[]);
  await ctx.close();await fast.close();console.log(JSON.stringify({status:'PASS',checks:['normal shuffle and cut wait for explicit continuation','all 78 cards reachable in 13 non-overlapping mobile windows','tap previews; confirm commits exactly once','candidate and page survive reload','selection preserves surrounding DOM','sequential 900ms reveal then unlimited viewing time','final reveal survives reload before acknowledgement','whole-spread result with optional deselectable cards','normal and reduced/skip paths preserve identical deck and outcome','English ritual screens at 320px have no overflow or untranslated text']}));
 }finally{await browser.close();if(server){server.closeAllConnections();await new Promise(r=>server.close(r));}}
})().catch(e=>{console.error(e);process.exitCode=1;});
