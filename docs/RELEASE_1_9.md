# 1.9.0 中文学习与解读修复

状态：本地发布候选已通过完整检查与独立走查；提交、PR、CI、合并、服务器切换和公网复测仍须分别记录，不能由本地结果代替。

## 本版结果

- 公共产品只显示中文。旧 `lang=en` 链接和历史语言状态回落中文；页面没有英文切换入口，稳定 ID、三类本地存储和旧记录继续兼容。
- 首页与抽牌目录压缩无效留白，移动端首屏能看到主要任务；牌阵目录先显示用途短述，详情再显示完整用途、牌位和问题范围，并按牌数由少到多排列。
- 课程与 78 张牌库分开。现有 20 课按初级、中级、高级组织；每张牌先讲核心意思、画面线索和记忆方法，再练正位、整牌理解、逆位和间隔回访。完成正位、逆位与两次到期回访后才标记为已掌握。
- 学习模型共 98 个单元、724 题。381 条已审查错误选项均改为指出所选理解的问题并给出具体判断依据；错误反馈不得复制正确反馈，同一张牌两个整牌错误理解也必须分别纠正。
- 独立牌库包含全部 78 张牌、搜索和花色筛选；牌面详情补充核心牌义、正逆位、画面、数字或角色、场景示例、常见误读和近似牌区别，默认收起较长资料。
- 抽牌问题保持可选；未主动提问时按牌阵提供完整本地解读。预设问题与用户问题冲突会在洗牌前说明并让用户明确选择，不由系统暗中决定。
- 78 张牌在密集连续牌扇中全部可达，第一张完整显示；已选、揭牌与结果阶段都保持实际牌阵位置。补充问题后沿用原牌重新生成本地解读，旧 AI 回答保留在历史中。
- 离线解读先回答实际所问，再说明牌位、条件和行动；Yes/No 不以正逆位计票，二择一比较同一维度的两条路径，自由三张不擅自套成过去／现在／未来或其他固定角色。
- AI 只在用户点击“请 AI 深度解析”后建立临时会话并发送。服务端使用 RP-1.2.0，校验问题领域、牌阵范围与自由三张牌位；高风险问题直接给现实求助提示，不调用模型。
- 学牌页面没有播放、重播、看动效或跳过动效；洗牌、选牌入位和整组揭牌的必要反馈自动完成，并支持减少动态效果。

## 本地验证证据

- `npm test` 从头运行通过；构建指纹为 `1.9.0-eedfca7ccc1d6715`，离线包包含 121 项资源。
- 课程构建通过：20 课、78 张牌、98 个单元、724 题、5011 组双语源文本；英文源只用于内容维护，公共界面仍固定中文。
- 学习反馈门禁通过：381 条审查项与最终内容一致，错误反馈复制正确反馈为 0；78 张牌整牌题的两个错误反馈重复为 0；旧机械模板残留为 0。
- 解读边界通过：25 个场景、32 个当前及兼容牌阵、本地整组解读、问题冲突、编辑后重解和 AI 模拟契约通过。72 个高风险表达与 20 个安全近似句在离线和服务端路径通过，测试未调用真实供应商。
- Chromium 与 WebKit 的 320、390、430、768、1280px 流程通过；320×568 下 78 张牌全部挂载并可达，牌阵、牌库、详情和结果页无横向溢出。二择一和凯尔特十字在选中、揭牌、结果三个阶段保持同一结构。
- v1.0 到 v1.9 的升级、缓存中断、旧记录、离线重开及三个存储键兼容检查通过。
- 解读与 UI 各完成一次独立只读复审；当前未发现发布阻断。独立复审不替代真实手机和长期模型质量验证。

## 发布后仍需验证

- 记录 PR 检查、主分支检查、合并提交、GitHub Pages、服务器发布目录与 HTTPS 公网结果。
- 用不含私人信息的固定虚构问题做一次生产 AI 深度解析，核对先回答问题、牌位正确、语言自然且没有越界保证。
- 浏览器视口检查不能替代真实 iPhone、微信 WebView、大字号和屏幕阅读器验收。
- 自动检查与内容覆盖不能证明每位使用者一定学会 78 张牌；仍需新人试学、延迟回忆和独立塔罗教师审稿。
- 固定合成问题通过不能证明模型每次输出都正确，生产监控仍需持续观察拒答、超时和错误牌位输出。
